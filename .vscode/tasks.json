{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "mkdir -p bin && g++ main.cpp -Ipngio -lpng -g -o bin/program",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}